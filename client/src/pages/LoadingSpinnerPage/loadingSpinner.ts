import styled from 'styled-components'
import { Box } from '@mui/material'

export const LoadingSpinnerBox = styled(Box)`
  display: flex;
  position: absolute;
  top: calc(50vh - 100px);
  left: calc(50vw - 100px);
`
